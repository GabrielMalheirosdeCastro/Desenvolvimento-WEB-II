# Plano de Ação — v2.0.0: Versão Final + Entrega Acadêmica (MAJOR)

**Data:** 2026-06-15
**Solicitado por:** Gabriel Malheiros de Castro
**Contexto:** Marco final. Encerra os RNFs de operação que faltam (**D4 monitoria**, **D6
escalabilidade**), o **E2E completo via Playwright (E3)** e todo o **Bloco G** (fechamento acadêmico).
O salto para **MAJOR** marca a transição de "protótipo evolutivo" para **versão final entregável** à
banca. Pré-requisito: v1.14.0–v1.17.0 concluídas (todos os RFs e RNFs de qualidade fechados).

## Objetivo

Entregar a versão final de produção com:
- **E3** — Suíte E2E completa via Playwright MCP (na estação — VPS é headless).
- **D4/RNF05** — Monitoria/uptime ≥ 99,5% com alertas.
- **D6/RNF06** — Validação de escalabilidade sob carga (meta de referência: 10k simultâneos).
- **G1–G4** — Documentação acadêmica final (LaTeX no Overleaf, README/CHANGELOG, arquitetura, roteiro da banca).

## Etapas

### Bloco E3 — E2E completo (estação)
- [ ] 1. Confirmar servidor MCP `playwright` ativo na estação Windows (Seção 2.5 das instruções).
- [ ] 2. Expandir `tests/e2e/**`: jornadas críticas (login → dashboard → CRUD metas → bem-estar →
      chatbot → notificações → logout) com credenciais via env.
- [ ] 3. Rodar a suíte E2E completa na estação e registrar evidências.

### Bloco D4 — Monitoria/uptime (≥ 99,5%)
- [ ] 4. Configurar monitor de uptime para `/healthz` (ex.: Uptime Kuma self-hosted na VPS).
- [ ] 5. Configurar alertas (e-mail/Telegram) para indisponibilidade e erros 5xx.
- [ ] 6. Documentar SLO/SLA e o painel de monitoria.

### Bloco D6 — Escalabilidade sob carga
- [ ] 7. Definir cenário de carga (k6/autocannon) contra endpoints de leitura representativos.
- [ ] 8. Executar teste de carga **fora de horário** e medir latência/erros sob concorrência.
- [ ] 9. Documentar limites observados e recomendações (pool do Supavisor, índices, cache).

### Bloco G — Documentação e entrega
- [ ] 10. **G1** — Atualizar o documento LaTeX no **Overleaf** com o estado final (a IA **imprime os
      trechos em Markdown**; o aluno cola no Overleaf — regra 0.1, somente-leitura no `.tex` local).
- [ ] 11. **G2** — Finalizar `README.md` e `CHANGELOG.md` (lançar a seção `## [2.0.0]`).
- [ ] 12. **G3** — Diagramas de arquitetura atualizados pós-implementação (4 camadas, ER, fluxo real).
- [ ] 13. **G4** — Manual do usuário + roteiro de demonstração para a banca.

### Encerramento (release MAJOR)
- [ ] 14. Build + suíte completa (unit/integração/E2E) verde.
- [ ] 15. Bump **2.0.0** (3× `package.json`), `CHANGELOG.md`, `README.md`, badge na página.
- [ ] 16. Commits atômicos pt-BR → push → `node scripts/deploy.mjs` → `/version` = 2.0.0.
- [ ] 17. Validação visual final via Playwright MCP (badge `v2.0.0` em produção).

## Trilha paralela — Infra/DevOps (sem bump da SPA)

Executar como tarefas operacionais na VPS, independentes das versões da SPA:
- [ ] F4 — Snapshot da VPS antes de upgrades maiores (ex.: PG 17 → 18).
- [ ] F5 — Aumentar limite de RAM do `supabase-analytics` (operando ~97%).
- [ ] F6 — Avaliar Cloudflare em modo *proxied* (WAF/CDN) após validar integrações.
- [ ] F7 — Pipeline de `prisma migrate deploy` em produção (idealmente antes da 1ª migração nova de schema).

## Impacto Esperado

- Arquivos afetados: `tests/e2e/**`, configs de monitoria/carga (na VPS), `site_acolhimento_faesa.tex`
  (via Overleaf), `README.md`, `CHANGELOG.md`, `docs/**` (arquitetura, manual), 3× `package.json`.
- Pendências fechadas: E3, D4/RNF05, D6/RNF06, G1, G2, G3, G4 + trilha F4–F7 → **backlog 100% ✅**.
- README/CHANGELOG precisam ser atualizados? **Sim** (G2 é o fechamento formal).

## Riscos e Cuidados

- **`.tex` é somente-leitura local** — a IA nunca edita o arquivo; entrega trechos em Markdown para o
  aluno colar no Overleaf (regra crítica 0.1).
- Teste de carga (D6) pode impactar produção — executar em janela de baixa demanda e monitorar a VPS
  (sem swap thrashing; lembrar do OOM de build já mitigado).
- E2E (E3) só roda na estação — confirmar MCP Playwright ativo antes; **não** instalar GUI na VPS.
- O salto para MAJOR exige que **todas** as versões anteriores (1.14–1.17) estejam fechadas — não
  antecipar o bump 2.0.0 com pendências abertas.

## Critério de Conclusão

Todas as 53 pendências do backlog marcadas ✅, `/version` em produção retornando **2.0.0**, suíte
unit/integração/E2E verde no CI antes do deploy, monitoria ativa com alertas, e documentação acadêmica
(LaTeX + README/CHANGELOG + arquitetura + roteiro da banca) finalizada.
